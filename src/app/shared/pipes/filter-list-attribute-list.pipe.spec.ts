import { FilterListAttributeListPipe } from './filter-list-attribute-list.pipe';

describe('FilterListAttributePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListAttributeListPipe();
    expect(pipe).toBeTruthy();
  });
});
